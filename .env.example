# ==================================================
# 🏥 MediApp - Variables de Entorno
# ==================================================

# --------------------------------------------------
# 🐘 PostgreSQL Database Configuration
# --------------------------------------------------
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=mediapp_db
POSTGRES_USER=mediapp_user
POSTGRES_PASSWORD=super_secure_password_123
POSTGRES_SSL_MODE=disable

# --------------------------------------------------
# 🔐 Application Configuration
# --------------------------------------------------
APP_ENV=development
APP_PORT=8080
APP_DEBUG=true

# --------------------------------------------------
# 🔑 JWT Configuration (si usas autenticación)
# --------------------------------------------------
JWT_SECRET_KEY=JWT_SECRET_KEY
# --------------------------------------------------
# 📧 Email Configuration (si envías emails)
# --------------------------------------------------
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@mediapp.com

# --------------------------------------------------
# 🚨 Logging Configuration
# --------------------------------------------------
LOG_LEVEL=debug
LOG_FORMAT=json

# --------------------------------------------------
# 🌐 API Configuration
# --------------------------------------------------
API_VERSION=v1
API_BASE_PATH=/api/v1

# ==================================================
# 💡 Tips:
# 1. Copia este archivo a .env y modifica los valores
# 2. Nunca commitees el archivo .env
# 3. Usa contraseñas seguras en producción
# ==================================================

# Variables para Docker Compose
FRONTEND_PORT=3000
BACKEND_PORT=8080
DB_PORT=5432

# Estas variables serán usadas por tu aplicación Go para conectarse a Vault
VAULT_ADDR=http://127.0.0.1:8200
VAULT_TOKEN=dev-token
# This file was auto-generated by the Firebase CLI
# It is designed to be used with the Firebase Hosting GitHub Action
# This workflow assumes your site is a Single Page Application (SPA) located in the 'dist' folder.

name: Deploy to Firebase Hosting on Merge
'on':
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_MEDIAAPP_469600 }}'
          projectId: mediaapp-469600
          entrypoint: ./